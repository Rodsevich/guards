import 'package:guards/guards.dart';

/// A guard used to demonstrate documentation for a public member.
abstract base class ExampleGuard extends GuardBase {
  /// Constructs a new instance of [ExampleGuard].
  ExampleGuard({super.persistenceDelegate});
}
